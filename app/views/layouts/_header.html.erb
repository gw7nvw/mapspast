    <header class="navbar navbar-fixed-top navbar-inverse">
      <div class="navbar-inner" style="font-size:0.8em">
<!--        <div class="container"> -->
        <div id="our_logo">
          <%= link_to "MapsPast", root_path, id: "logo" %>
        </div>
          <nav>
            <ul class="nav pull-right" id="menus">
              <li id="fat-menu" class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                   Menu <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to 'Home', root_path, remote: true, :id => 'home_link', :onclick => "linkHandler('homes_link')"  %></li>
                  <li><%= link_to 'Discuss', '/forum/1/', remote: true, :id => 'forum_link', :onclick => "linkHandler('forum_link')"  %></li>
                  <% if signed_in? %>
                    <li> <%= link_to "Sign out", logout_path, method: "delete" %> </li>
                  <% else %>
                  <li><%= link_to 'Login', login_path, remote: true, :id => 'login_link', :onclick => "linkHandler('login_link')"  %></li>
                  <% end %>

                </ul>
              </li>
            </ul>
          </nav>
        </div>
      </header>
