</li><header class="navbar navbar-fixed-top navbar-inverse">
<div class="navbar-inner" style="font-size:0.8em">
<!--        <div class="container"> -->
        <div id="our_logo">
          <%= link_to "MapsPast", root_path, id: "logo" %>
        </div>
          <nav>
            <ul class="nav pull-right" id="menus">
              <li id="fat-menu" class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                   Mapsheets <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to 'Browse', '/mapsheet', remote: true, :id => 'home_link', :onclick => "linkHandler('homes_link')"  %></li>
                  <% if signed_in? %>
                    <li><%= link_to 'Create', '/mapsheetinfo', remote: true, :id => 'home_link', :onclick => "linkHandler('homes_link')"  %></li>
                  <% end %>
                </ul>
              </li>
                  <li><%= link_to 'Maptalk', '/maptalk/1/', remote: true, :id => 'forum_link', :onclick => "linkHandler('forum_link')"  %></li>
                  <li><%= link_to 'Mapnews', '/mapnews', remote: true, :id => 'news_link', :onclick => "linkHandler('news_link')"  %></li>
              <li id="fat-menu" class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                   Menu <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to 'About', '/about', remote: true, :id => 'home_link', :onclick => "linkHandler('home_link')"  %></li>
                  <% if signed_in? %>
                    <li><%= link_to 'Profile', '/users/'+@current_user.id.to_s, remote: true, :id => 'user_link', :onclick => "linkHandler('user_link')"  %></li>
                    <li> <%= link_to "Sign out", logout_path, method: "delete" %> </li>
                  <% else %>
                  <li><%= link_to 'Login', login_path, remote: true, :id => 'login_link', :onclick => "linkHandler('login_link')"  %></li>
                  <li><%= link_to 'Sign Up', signup_path, remote: true, :id => 'signup_link', :onclick => "linkHandler('signup_link')"  %></li>
                  <% end %>

                </ul>
              </li>
              <li>
                <%= link_to image_tag("/assets/download.png",   :class => 'dock-item'), '/print/', remote: true, onclick: "linkHandler('print')", id:  "print", title: "Download / print current map area"  %>
               </li>
 
          </ul>
          </nav>
        </div>
      </header>
