if (window.history.pushState!=null) window.history.pushState(null, 'MapsPast | Mapseries | Sheet', '/seriessheet/<%=params[:id]%>');
$('#left_panel').html("<%= escape_javascript(render(:partial => 'seriessheet/show')).html_safe %>");
