if (window.history.pushState!=null) window.history.pushState(null, 'MapsPast | <%@mtitle%>', '/<%=@ptitle%>/<%=params[:id]%>');
$('#left_panel').html("<%= escape_javascript(render(:partial => 'maptalk/show')).html_safe %>");
