if (window.history.pushState!=null) window.history.pushState(null, 'MapsPast', '/login');
$('#left_panel').html("<%= escape_javascript(render(:partial => 'sessions/new')).html_safe %>");
init_resize();

