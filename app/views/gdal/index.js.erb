if (window.history.pushState!=null) window.history.pushState(null, 'MapsPast | GIS | GDAL / QGIS', '/gdal/');
$('#left_panel').html("<%= escape_javascript(render(:partial => 'gdal/index')).html_safe %>");
