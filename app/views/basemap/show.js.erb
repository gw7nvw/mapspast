if (window.history.pushState!=null) window.history.pushState(null, 'MapsPast | Mapseries', '/basemap/<%=params[:id]%>');
$('#left_panel').html("<%= escape_javascript(render(:partial => 'basemap/show')).html_safe %>");
