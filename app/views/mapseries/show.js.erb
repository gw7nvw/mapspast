if (window.history.pushState!=null) window.history.pushState(null, 'MapsPast | Mapseries', '/mapseries/<%=params[:id]%>');
$('#left_panel').html("<%= escape_javascript(render(:partial => 'mapseries/show')).html_safe %>");
