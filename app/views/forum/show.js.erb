if (window.history.pushState!=null) window.history.pushState(null, 'MapsPast | Forums', '/forum/<%=params[:id]%>');
$('#left_panel').html("<%= escape_javascript(render(:partial => 'forum/show')).html_safe %>");
