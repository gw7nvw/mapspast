if (window.history.pushState!=null) window.history.pushState(null, 'MapsPast | Mapsheet', '/mapsheet/<%=params[:id]%>');
$('#left_panel').html("<%= escape_javascript(render(:partial => 'mapsheet/show')).html_safe %>");
